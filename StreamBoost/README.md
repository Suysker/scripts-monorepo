# 流媒体加速缓冲 🚀

> **通用流媒体加速缓冲脚本：扩大缓冲、并发预取、内存命中、在途合并、站点级启停，一把梭！**
> 现已深度适配 **HLS.js**，后续可拓展到更多播放器/协议。

![License](https://img.shields.io/github/license/Suysker/scripts-monorepo?style=flat-square) ![Downloads](https://img.shields.io/greasyfork/dt/507274?style=flat-square) ![Version](https://img.shields.io/greasyfork/v/507274?style=flat-square) ![Tampermonkey](https://img.shields.io/badge/Tampermonkey-5.1.1-red.svg?style=popout-square) ![Tampermonkey](https://img.shields.io/badge/Tampermonkey-Beta-red.svg?style=popout-square)

---

## 📁 仓库位置（Monorepo）

- [Monorepo 根目录](../)
- [当前项目目录](./)
- [GitHub 仓库](https://github.com/Suysker/scripts-monorepo)

---

## 🐣 这个脚本为什么会诞生？

在线看剧/直播时，很多站点的**缓冲策略非常保守**：只往前拉一小段，稍微网络抖一下就**卡**；有些站点还把 HLS 片段加载做成了**串行**（同时只跑一个请求），遇到高 RTT/限速就雪上加霜。
更糟的是，部分站点自定义了 Loader/XHR，导致**明明 HTTP/2 支持多路复用，却看起来只有一个在下载**；偶发 403/CORS 也会让播放器反复重试、白白等很久。
这些体验都很烦躁，于是就有了 **StreamBoost**：在不侵入页面业务逻辑的前提下，接管片段的**预取与缓存策略**，把能并发的并发起来，把能命中的命中起来，把没必要的在途请求**及时中止**，尽量把“卡顿感”消掉。

---

## 🌟 主要功能

* **并发预取（真正并发）**：对齐 `FRAG_LOADING / FRAG_LOADED` 连续向前预取（默认 12 段）。
  优先 **原生 XHR**（可沿用站点 `xhrSetup`，避免站点自定义 Loader 造成“串行化”）；再回退 **Hls 内置 loader**；最后 **fetch**。
* **在途合并**：播放器要的片段如果**已经在下载**，短暂等待直接复用结果，避免重复请求。
* **内存 LRU 命中（fLoader）**：下载过的片段进入 LRU；命中后直接回填，几乎零延迟。
* **淘汰过时在途**：level/sn 前进时，自动 `abort` 落后的在途下载，省流量也省时间。
* **缓冲增强（HLS）**：向上提高 `maxBufferLength / maxMaxBufferLength / maxBufferSize / backBufferLength` 目标；点播会在 `LEVEL_LOADED` 后再次确认，直播保留站点的 live-sync 参数。
* **真正全局的设置**：参数、全局开关与 Debug 状态保存在脚本管理器的 GM 存储中，同一脚本安装下的所有匹配网站共享。
* **按当前主机名启停**：主机名禁用策略与全局开关彼此独立；只跳过命中的文档主机名。
* **iframe 配置一致**：顶层页面、同源 iframe 与由 `@match` 独立注入的跨域 iframe 都读取同一份全局配置。
* **调试日志**：打开后关键决策全可见，排查问题更直观。

---

## 📥 安装方式

1. 安装浏览器扩展 **Tampermonkey**（或兼容脚本管理器）。
2. 打开 `StreamBoost/StreamBoost.js`，点 **Raw** 进行安装（或在 Greasy Fork 页安装）。
3. 刷新含播放器的页面即可体验加速。

脚本源码地址（Raw）：
`https://raw.githubusercontent.com/Suysker/scripts-monorepo/main/StreamBoost/StreamBoost.js`

> 兼容：Chrome / Edge / Firefox（现代版本）。Safari 因扩展限制可能需要额外授权。

---

## ⚙️ 菜单与控制

安装后，在 Tampermonkey 图标的页面菜单里可见：

* `🔌 全局状态（启用/停用）`
* `⛔ 停用当前主机名` / `✅ 移除当前主机名停用规则`
* `⚙️ 打开参数配置页`
* `🐞 Debug 日志（启用/停用）`

`全局状态`、`Debug` 和参数配置由所有匹配网站与 iframe 共享；`当前主机名` 命令只改变当前文档主机名的禁用策略。规则忽略协议和端口、精确区分不同子域名，并支持 IPv4 / IPv6 主机名。跨域播放器 iframe 是另一个文档主机名，不会因为顶层网站被单独禁用而自动禁用。

> 设置保存或菜单切换后不会热修改已经创建的 Hls 实例。请刷新所有需要应用新配置的顶层页面或 iframe；新打开的匹配页面会直接读取最新配置。

---

## 🛠 全局参数配置（GM 存储）

请通过菜单里的**参数配置页（悬浮可视化表单）**修改参数。配置以一个带版本号的对象保存在脚本管理器的 GM 存储中，不属于任何网站的网页存储；在网站 A 保存后，网站 B、同源 iframe 和跨域 iframe 刷新后都会使用同一份配置。

参数页提供滑动条、选项按钮和启用/停用按钮，并按 `预取并发`、`缓冲与内存`、`请求策略+常规开关` 分组。`恢复默认` 只恢复面板中的运行参数；全局启停和主机名禁用策略仍由各自的菜单命令管理。完成修改后需点击 `保存全局配置`，再刷新需要生效的页面。

### 控制项与作用域

| 控制项 | 持久化作用域 | 实际作用 |
| --- | --- | --- |
| 全局状态 | 当前脚本安装全局 | 关闭后，所有匹配网站和 iframe 刷新时都不再注入 StreamBoost |
| Debug 日志 | 当前脚本安装全局 | 所有匹配网站和 iframe 刷新后统一开启或关闭日志 |
| 参数配置页 | 当前脚本安装全局 | 所有匹配网站和 iframe 读取相同参数 |
| 当前主机名停用/启用 | 全局主机名策略列表 | 只决定与当前文档主机名匹配的页面是否注入，不改变全局状态 |

### 参数配置页（常规开关）

| 参数 | 说明 | 默认值 |
| --- | --- | --- |
| 并发预取 | 连续向前预取后续片段 | 启用 |
| 内存命中 fLoader | 优先复用页面上下文中的 LRU 缓存和在途结果 | 启用 |

### 参数配置页（进阶参数）

| 参数 | 范围/说明 | 默认值 |
| --- | --- | --- |
| 预取前瞻片段数 | `0-60` | `12` |
| 页面上下文总并发上限 | `1-16`；在每个顶层页面或 iframe 的独立运行上下文内执行 | `4` |
| 单资源 Origin 并发上限 | `1-16`；限制单个片段资源 Origin 的并发 | `4` |
| 预取超时 | `1000-120000 ms` | `15000 ms` |
| 在途复用等待 | `0-10000 ms` | `500 ms` |
| 预取策略 | XHR、Hls Loader 与 fetch 的尝试顺序 | `xhr-hls-fetch` |
| HLS 前向缓冲目标（至少） | `60-3600 秒`；将 HLS 的前向缓冲上限提高到至少该值，适用于直播与点播 | 低内存设备 `180`，其他设备 `600` |
| 回看缓冲目标（至少） | `0-1800 秒`；不降低站点已有的更高值 | `180` |
| 最大缓冲目标（至少） | `120-7200 秒`；将 HLS `maxMaxBufferLength` 提高到至少该值 | `1800` |
| LRU 上限 / MSE 目标 | `16-512 MB`；LRU 以该值为严格上限，HLS `maxBufferSize` 至少提高到该值且内部最低为 `60 MB` | 按设备内存取 `64/128/192 MB` |

虽然这些参数全局持久化，但预取队列、在途表和 LRU 缓存分别存在于每个页面上下文中。因此，“页面上下文总并发上限”和 LRU 上限不是跨标签页、跨 iframe 汇总的浏览器级限额。缓冲与 MSE 数值用于向上增强：直播和点播都会使用这些目标，但脚本不会用较小的用户值压低站点原本更高的 HLS 配置，也不会改写直播同步参数。

### 从旧版 localStorage 显式导入

旧版曾把 `HLS_BIGBUF_*` 参数保存在当前网页 origin（协议、主机名和端口）的 `localStorage` 中。新版在正常启动和播放运行时不会读取、自动导入或回退到这些值；只有用户打开参数页时，面板才会检测当前 origin 是否存在可显式导入的旧参数。这样可避免任意网站或互相冲突的旧配置静默成为全局设置。

如果当前 origin 存在可识别的旧版运行参数，面板会提供“导入本站旧参数”操作：

1. 导入仅把经过白名单校验和范围规范化的运行参数填入表单。
2. 导入本身不会持久化；检查结果后仍需点击 `保存全局配置`，才会写入全局 GM 配置。
3. 旧版的全局开关、Debug 和黑名单状态不会被导入。
4. 原有 localStorage 键不会被删除，便于必要时降级；正常运行会忽略它们。

### 快速验证（手动）

1. 在网站 A 打开参数页，将“页面上下文总并发上限”等参数改成容易辨认的值，保存并刷新。
2. 打开无关的网站 B，确认参数页显示同一值；刷新 HLS.js 播放页并用 Debug 日志确认运行参数一致。
3. 在含跨域播放器 iframe 的页面刷新顶层页面，确认 iframe 的 StreamBoost Debug 日志也使用同一份参数。
4. 在网站 B 的开发者工具中写入冲突的旧 `HLS_BIGBUF_*` localStorage 值并刷新，确认它不会覆盖全局配置。
5. 用“停用当前主机名”确认只跳过该主机名，再用“全局状态”确认所有匹配网站和 iframe 刷新后都停止注入。
6. 如面板检测到旧参数，执行显式导入，先检查表单，再保存并到另一网站确认导入结果已成为全局参数。

---

## ✅ 适用范围与限制

* **适用**：采用 **HLS.js** 的站点（含以 video.js 方式集成的 HLS）。
* **限制**：DRM/加密媒体、Safari 原生 HLS（非 MSE/Hls.js）不在接管范围。
* **网络策略**：跨域仍受 CORS 约束；原生 XHR 会遵循站点 `xhrSetup`（可设置 header / `withCredentials`）。
* **生效时机**：设置不会热更新现有 Hls 实例；保存或切换后需要刷新相关页面或 iframe。
* **配置边界**：GM 配置在同一脚本管理器安装内全局共享，不代表不同浏览器、浏览器配置文件或设备之间自动同步。
* **主机名边界**：当前主机名禁用忽略协议和端口、区分子域名；跨域播放器 iframe 需按其自身主机名管理，或使用全局开关统一停用。
* **资源边界**：并发、在途请求和 LRU 缓存按页面上下文独立执行，不是浏览器级统一资源池。

---

## ❓常见问题（FAQ）

* **看起来还是“串行”？**
  有些站点/中间层在 HTTP/2 下会把多请求显示为“挂起”，其实是多路复用；开启 `🐞Debug` 可看到 StreamBoost 的并发占位与在途数量，更准确。
* **403 / CORS 怎么办？**
  预取优先原生 XHR，并把机会交给站点 `xhrSetup`；若资源服务器不允许跨域，仍需 CORS 许可。
* **会不会很占内存？**
  LRU 缓存按每个页面上下文的配置上限逐段淘汰。HLS `maxBufferSize` 则是向上增强目标，至少为 `60 MB`，不会压低站点已有的更高值；浏览器和 HLS.js 仍会共同决定实际 MSE 占用。可在参数页调整“LRU 上限 / MSE 目标”。
* **为什么保存后当前视频没有立即变化？**
  已创建的 Hls 实例和 Loader 无法安全热替换。刷新顶层页面或对应 iframe 后，新实例才会使用最新全局配置。
* **旧版 localStorage 设置还会自动生效吗？**
  不会。只有在参数页主动导入、检查并点击保存后，受支持的旧运行参数才会成为全局设置。

---

## 📄 许可协议

本脚本遵循 [MIT License](LICENSE)。

---

## 🤝 反馈与贡献

* 项目地址：
  [Monorepo 根目录](../) / [当前项目目录](./) / [GitHub 仓库](https://github.com/Suysker/scripts-monorepo)
* 提交 Issue / PR：**GitHub Issues / Pull Requests**
* Issues 入口：`https://github.com/Suysker/scripts-monorepo/issues`
* 欢迎提供站点兼容性、复现步骤与优化建议。喜欢就点个 ⭐ Star，感谢支持！

